import React from "react";

export const metadata = {
  title: {
    absolute: "Contact",
  },
  description: "Contact page for super next hero website",
};

const page = () => {
  // console.log(name);
  return (
    <div>
      <h1>This is contact page</h1>
    </div>
  );
};

export default page;
